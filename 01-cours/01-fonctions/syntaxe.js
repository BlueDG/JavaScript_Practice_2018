// 1. La syntaxe "variable"
let additionner = function (nombre1, nombre2){
    // let resultat = nombre1 + nombre2;
    // return resultat;
    
    return nombre1 + nombre2;
    // ça c'est une syntaxe variable.
    // La même chose mais écrite différemment.
}

// 2. La syntaxe "fléchée" (fonction fléchée)
let additionner2 = (nombre1, nombre2) => nombre1 + nombre2;

// utile lorsque l'on n'a qu'une seule action

// 3. La syntaxe classique voir historique
function additionner3 (nombre1, nombre2){
    return nombre1 + nombre2;
}

